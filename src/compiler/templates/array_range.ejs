(function() {
  var acc = [];
  <% if(Number(end) > Number(start)) { %>
  for(var i = <%= start %>; i <= <%= end %>; i++) {
  <% } else { %>
  for(var i = <%= start %>; i >= <%= end %>; i--) {
  <% } %>
    acc.push(i);
  }
  return acc;
})()